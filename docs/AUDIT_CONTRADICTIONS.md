# Contradiction audit (post metric-16 fix)

**Date:** August 18, 2026

## Fixed earlier

| Issue | Status |
|-------|--------|
| Metric $16\ell_0^2$ vs $4\ell_0^2$ | **Fixed** (papers stubbed/corrected) |
| $A_5$ as metric isometries | **Rejected** in STATUS / core paper |
| RH as theorem in old papers | **Bannered** proposal only |

## Remaining / newly clarified

### 1. Dual-Zero “triviality” language (foundations file)

**Conflict:** Historical foundations text said $\mathrm{st}(\varepsilon)=0$ is immediate and could be read as “Dual-Zero does nothing.”

**Resolution:** Doc 18 rewritten. Novelty is dual averaging of **spectral data** with geometric $\omega_0$, from original Dual-Zero math — not a non-zero standard part of $n^{-n}$.

### 2. Two writings of $\operatorname{Reg}_2$

| Form | Where |
|------|--------|
| $(a_{2n}+a_{2n+1})/2$ | Doc 18 (sequences) |
| $(f(\lambda+\delta)+f(\lambda-\delta))/2$ | Dual-Zero arithmetic paper |

**Resolution:** Same principle, two domains; both locked in updated doc 18.

### 3. $\omega_0=(R/D)^{4/13}$

**Status:** Form is geometric; **exponent $4/13$** is not fully derived in-repo from first principles. Residual — do not pretend the exponent is a theorem.

### 4. $G_N=64\pi\ell_0^2/45$ vs scheme table

**Status:** Proportionality $G_N\propto\ell_0^2$ is structural. Pure prefactor is convention-locked (Note XXIII). Not a contradiction if stated that way; **is** a contradiction if any file claims scheme-free digit identity for $\alpha$.

### 5. Missing hardening doc 37

README authority table referenced scheme-comparison doc 37; file may be absent or renamed. **Action:** treat doc 39 + 18 as Dual-Zero scheme authority until 37 is restored or the README pointer is removed.

### 6. Phenomenology overclaims in old paper *bodies*

Many papers were replaced by stubs. Any remaining full-text historical file still on disk that claims “full SM derived” or exact $m_H$ digits is **superseded by STATUS**, even if a paragraph remains.

### 7. Yukawa / CKM digits vs core paper

Core paper does **not** claim digit-level Yukawas. STATUS / public summary must not drift back into claiming them as locked.

---

## Dual-Zero novelty (authoritative short form)

Dual-Zero is the original information-conserving dual regularization:

- generator $\varepsilon(n)=\omega_0(-1)^n n^{-n}$
- dual average $\operatorname{Reg}_2$
- geometric $\omega_0$
- acts on spectral towers, not as “$\mathrm{st}(n^{-n})\neq 0$”

---

*Contradiction audit August 18, 2026.*
