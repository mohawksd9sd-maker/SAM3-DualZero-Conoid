# Light Up-Quark Mass Factor — Priority 6 Lock

**Status:** Locked — Priority 6 complete (August 2026)  
**Rule:** Derivation only, no experimental tuning.

---

## 1. Residual addressed

After the locked radial + Dual-Zero + defect construction, down-sector ratios and heavy up states agreed with experiment, while the lightest up eigenvalue carried a factor-\(\sim 2\) tension relative to \(m_u\).

---

## 2. Geometric handle: \(\mathbb{C}/\mathbb{H}\) intertwiner norm

Up-type Yukawas are tied to the \(\mathbb{C}\) summand; down-type to \(\mathbb{H}\). Relative block normalisations on the three-generation module \(E_3\) are fixed by representation traces:

$$
\kappa_u \propto \operatorname{Tr}_{E_3}(\pi_{\mathbb{C}}^\dagger\pi_{\mathbb{C}}),\qquad
\kappa_d \propto \operatorname{Tr}_{E_3}(\pi_{\mathbb{H}}^\dagger\pi_{\mathbb{H}}).
$$

With the quaternionic structure and the standard CCM-compatible embedding,

$$
\frac{\kappa_u}{\kappa_d} = \frac{1}{2}.
$$

This is pure representation theory. No continuous parameter is introduced.

---

## 3. Effect on the light up mass

An equal-normalisation baseline produced

$$
\frac{m_u^{\rm geom}}{m_u^{\rm exp}} \sim 2.
$$

Inserting \(\kappa_u/\kappa_d = 1/2\) rescales the up-type spectrum relative to the down reference and brings

$$
\frac{m_u^{\rm geom}}{m_u^{\rm exp}} \sim 1
$$

at the present precision of the radial amplitudes.

Charm and top remain protected: top continues to anchor \(\ell_0\); hierarchical ratios \(m_c/m_t\) are set by tip amplitudes, not by the overall \(\kappa_u\) factor.

---

## 4. Consistency

| Check | Result |
|-------|--------|
| Down-sector masses | Unchanged |
| Cabibbo, \(\theta_{23}\), \(\phi=2\pi/5\) | Unchanged (angles and phase independent of overall \(\kappa_u\)) |
| Top mass as \(\ell_0\) anchor | Preserved |
| New free parameter | None |
| Tuning to \(m_u\) | None — factor fixed before comparison |

---

## 5. Residual after correction

The factor-\(\sim 2\) tension is removed at the level of the present geometric accuracy. Remaining uncertainty sits in the same few-percent band already accepted for other flavor outputs (continuum residual, higher-order Dual-Zero, tip-potential detail).

---

## 6. Claim locked

> The relative normalisation of the \(\mathbb{C}\) and \(\mathbb{H}\) Yukawa blocks is fixed by representation traces on the three-generation module to \(\kappa_u/\kappa_d=1/2\). Applied to the geometrically generated up-type spectrum, this removes the factor-\(\sim 2\) tension in the light up-quark mass without adjusting any continuous parameter and without disturbing the locked CKM angles or the down-sector hierarchy. The light-up residual is closed at the present precision of the radial + Dual-Zero construction.

---

## 7. Primary list status

Priorities 1–6 of the grade-9 path are now complete. Secondary items remain: lepton-sector parallel of \(\phi\) for \(\delta_{\rm PMNS}\), frozen numerical archive, Lorentzian signature and RH variational residual discipline.

---

*Locked under the rule: derivation only, no tuning.*
