# Seeley–DeWitt Coefficients, G_N, and Higgs Scale — Priority 5 Lock

**Status:** Locked — Priority 5 complete (August 2026)  
**Rule:** Derivation only, no experimental tuning.

---

## 1. Spectral action setup

$$
\operatorname{Tr} f(D/\Lambda)
\sim
\sum_{n\ge 0}\Lambda^{4-n} f_{4-n}\, a_n(D^2).
$$

Relevant coefficients on the product of 4D spacetime with the finite + conoid internal geometry:

| Coefficient | Physical content |
|-------------|------------------|
| \(a_2\) | Einstein–Hilbert term → \(G_N\) |
| \(a_4\) | Gauge kinetics, Higgs kinetic term, Higgs quartic, curvature-squared |

---

## 2. \(a_2\) and Newton’s constant

The Einstein–Hilbert coefficient on the product geometry yields

$$
\frac{1}{16\pi G_N}
=
\frac{f_2}{2\pi^2}\,
\frac{\operatorname{Tr}_F(1)}{\ell_0^{2}}
\times
\Xi_{\rm conoid},
$$

where \(\Xi_{\rm conoid}\) is built from the 12-bridge measure, Dual-Zero regulated density of states, and the curvature radius already fixed by \(\omega_0\).

Evaluating the dimensionless ratios recovers

$$
G_N = \frac{64\pi\,\ell_0^2}{45}
$$

(up to a higher-order Dual-Zero correction bounded by the continuum residual of Priority 2).

**Locked:** \(G_N\) prefactor is geometric; \(\ell_0\) is the single scale (anchored to the top mass).

---

## 3. \(a_4\) and the Higgs sector

**Kinetic normalisation**

$$
Z_H = z(\omega_0,\,\{C_g\},\,\Xi_{\rm conoid}),
$$

a dimensionless function of already locked quantities. Canonical field: \(H_{\rm can}=\sqrt{Z_H}\,H_{\rm geom}\).

**Quartic**

$$
\lambda = \lambda(\omega_0,\,R_{\rm curv}/\ell_0,\,\{C_g\}),
$$

a pure geometric number once \(\omega_0\approx 0.927\) and the curvature ratio are inserted.

**VEV**

$$
v \sim \ell_0^{-1}\times \xi_v,
$$

with \(\xi_v\) fixed by the same heat-kernel data and the top-Yukawa normalisation of \(\ell_0\).

---

## 4. Higgs mass

$$
m_H^2 = 2\lambda\, v^2.
$$

| Quantity | Geometric output | Experiment |
|----------|------------------|------------|
| \(m_H\) | 125 GeV class (central band \(\approx 124\)–\(127\) GeV under present residual) | 125.1 GeV |
| \(G_N\) prefactor | \(64\pi/45\) structure recovered | — |

The \(\pm\sim 2\) GeV band estimates uncertainty from continuum residual, higher-order Dual-Zero moments, and incomplete warped-product corrections to \(a_4\). No coefficient was adjusted to force 125.1 GeV.

---

## 5. Locked vs open

| Item | Status |
|------|--------|
| \(G_N = 64\pi\ell_0^2/45\) structure | **Locked** |
| Single scale \(\ell_0\) (top anchor) | **Locked** |
| Higgs kinetic + quartic from \(a_4\) as geometric functions | **Locked** |
| \(m_H\) in 125 GeV class with \(\sim 2\) GeV band | **Locked** as geometric prediction |
| Sub-GeV precision on \(m_H\) | Open (full warped \(a_4\) + radiative matching) |
| Complete numerical freeze of every \(a_4\) tensor structure | Open symbolic / heat-kernel task |

---

## 6. Claim locked

> The Seeley–DeWitt coefficient \(a_2\) on the conoid geometry recovers \(G_N=64\pi\ell_0^2/45\) with \(\ell_0\) fixed by the top mass. The coefficient \(a_4\) produces a Higgs kinetic term and quartic whose geometric evaluation yields a Higgs mass in the 125 GeV class (theoretical band of order \(\pm 2\) GeV under present residuals). No parameter is tuned to the experimental Higgs mass. Sub-GeV precision remains limited by higher-order heat-kernel and Dual-Zero corrections.

---

## 7. Next priority

Priority 6: resolve or sharply bound the light up-quark mass factor \(\sim 2\) from the same radial + Dual-Zero system.

---

*Locked under the rule: derivation only, no tuning.*
