# Numerical Production Readiness — Section 2 Status

**Status:** Partial lock (August 2026)  
**Rule:** No retuning; geometric `omega0 = 0.927` only.

---

## 1. Goals of §2

1. Production-grade 2D APS eigensolver  
2. End-to-end pipeline regenerating locked numbers from pure geometry  
3. Cleanup of `omega0 ≈ 0.97` legacy defaults

---

## 2. What was completed in this pass

| Item | Result |
|------|--------|
| **Single source of geometric truth** | `code/sam3_geometry_constants.py` — `OMEGA0_GEOMETRIC = 0.927`, Casimirs, η_ij, φ, G_N prefactor |
| **Legacy omega0 guard** | Dirac prototype rejects omega0 outside the geometric band (blocks ~0.97 defaults) |
| **Pipeline regeneration** | `master_verification_pipeline.py` writes `00_geometry_from_first_principles.json` from constants module, then frozen archive, CKM check, Dirac status |
| **Dirac honesty** | Prototype no longer injects artificial exact zeros; labeled **not production APS** |
| **Documentation** | This note + updated `code/README.md` |

---

## 3. Production APS eigensolver — still residual

`code/full_2d_dirac_conoid.py` remains a **prototype**:

- Sector-wise radial discretisation, not full 2D APS operator on large domains
- No published convergence table of `|λ|_min` vs `u_max` generated by a production driver in-repo
- Continuum residual `< 10^{-3}` and gap → 0 remain **locked in docs/hardening/10** as analysis results, not auto-regenerated by this prototype alone

### Production roadmap (P1–P4)

| Step | Deliverable |
|------|-------------|
| **P1** | Full 2D finite-difference / FEM Dirac with correct spin connection on the conoid metric |
| **P2** | APS boundary implementation at tip + outer cutoff with controllable violation metric |
| **P3** | Domain-extension driver publishing `|λ|_min(u_max)` table + residual table |
| **P4** | 2I isotype projection of near-zero eigenvectors feeding defect overlaps |

Until P1–P4 ship, public claim language must say **prototype + analytic locks**, not “production eigensolver regenerates all continuum claims.”

---

## 4. What the pipeline *does* regenerate without human choices

From pure geometry / locked representation data:

- `n_bridges`, `delta_theta`
- `omega0 = 0.927`
- `C_g = (6/5, 1, 4/5)`
- `η_12, η_13, η_23` (as frozen continuum integrals — not re-integrated live yet)
- `κ_u/κ_d = 1/2`
- `φ = 2π/5`
- `G_N/ℓ_0² = 64π/45`
- Cabibbo formula `θ_12 ≈ η_12 × π/12` cross-check vs locked angle

Live re-integration of defect overlaps on a production grid is **P4-linked** residual.

---

## 5. omega0 policy (locked)

$$
\omega_0 = 0.927 \quad \text{(geometric)}
$$

Any code path defaulting to `≈ 0.97` is **forbidden**. The constants module and Dirac guard enforce this.

---

## 6. How to run

```bash
python code/master_verification_pipeline.py
```

Outputs under `pipeline_output/`:

- `00_geometry_from_first_principles.json`
- `01_frozen_archive.json`
- `02_ckm_locked.json`
- `03_dirac_status.json`
- `04_status_summary.txt`
- `conoid_2d_evals.npy` (if prototype succeeds)

---

## 7. Lock statement

> Section 2 establishes a pure-geometry constants module with `omega0 = 0.927`, an upgraded master pipeline that regenerates geometric locks without experimental retuning, and explicit **prototype** labeling of the Dirac code. A production-grade 2D APS eigensolver (P1–P4) remains residual. Continuum residual and gap claims continue to rest on docs/hardening/10 until the production solver ships.

---

*Next ordered block: §3 Precision phenomenology residuals (optional tightening), or §4 RH direction (already residual-disciplined).*
